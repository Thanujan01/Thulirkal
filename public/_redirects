## SPA fallback removed: wildcard redirect caused Cloudflare validation
# If you need single-page app routing, configure a Cloudflare Pages `routes`
# or use a Worker to serve `index.html` for non-file requests.
# Original problematic rule (removed):
# /*    /index.html   200

